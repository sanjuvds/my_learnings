
<% content_for :javascript do %>
<%= javascript_include_tag 'jquery-ui-timepicker-addon.js'%>

<% end %>



<%= form_for(@timesheet) do |f| %>
  <% if @timesheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timesheet.errors.count, "error") %> prohibited this timesheet from being saved:</h2>

      <ul>
      <% @timesheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= f.label :from_date %><br />
    
    <%= f.date_field :from_date %>
 </div>   
 <div class="field">
	<%= f.label :to_date %><br />
    
    <%= f.date_field :to_date %>
    
    
    <%#= f.input :date, :as => :string, :required => true, :label => "Start Date/Timing: ",
	  #:wrapper_html => { :class => "dateFieldText" },
	 # :input_html => {:class => 'start_dateTimePicker dateTimePicker floatLeft', :value => !f.object.date.nil? ? f.object.date.strftime("%d-%m-%Y %I:%M %P") : "" } 
	 %>
    
    
    
    <%
    # Rails.logger.info "QQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQ"
    # Rails.logger.info f.object.date.try(:strftime,"%m/%d/%Y")
    # Rails.logger.info "QQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQQ"
    
     %>
    
    <%#= f.date_field :date, as: :date, value: f.object.date.try(:strftime,"%mm/%dd/%yyyy") 
    %>
    <%#= f.date_field :date, as: :date,  :value => !f.object.date.nil? ? f.object.date.strftime("%d-%m-%Y %I:%M %P") : "" %>
    
    <%#= date_select("article", "written_on") 
    %>
    
    
    <%= f.hidden_field :employee_id, :value => current_employee.id %>
  </div>

  <div class="field">
    <%= f.label :hours %><br />
    <%= f.text_field :hours, autofocus: true %>
  </div>	


<div class="field">
    <%= f.label :remarks %><br />
    <%= f.text_area :remarks, autofocus: true %>
  </div>	

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
